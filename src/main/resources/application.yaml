spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:mysql://localhost:8889/library?useSSL=false
    username: library
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLInnoDBDialect
